/**
 * Created by ximing on 2018/8/1.
 */
module.exports = class Agent {
    constructor(app) {
        this.app = app;
    }

    async findAgent(agentID) {}

    async run(agent) {
        if (agent) {
        } else {
        }
    }
};
